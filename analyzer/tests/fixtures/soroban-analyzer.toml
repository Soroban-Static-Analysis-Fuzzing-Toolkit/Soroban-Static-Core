schema_version = "1"

# Silence the memory-growth detector entirely.
[rules.SOR-106]
enabled = false

# Downgrade missing-auth findings so `--fail-on error` passes.
[rules.SOR-101]
severity = "warning"
